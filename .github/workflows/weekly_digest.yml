name: Weekly Econ Digest

on:
  schedule:
    # Monday 08:50 UTC = 09:50 CET (winter) / 10:50 CEST (summer)
    # Change to '50 7 * * 1' after Mar 29 if you want exactly 09:50 CEST
    - cron: '50 8 * * 1'

  # Allow manual trigger for testing
  workflow_dispatch:

jobs:
  send-digest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Restore seen papers cache
        uses: actions/cache@v4
        with:
          path: data/seen.json
          key: seen-papers-v2-${{ github.run_number }}
          restore-keys: |
            seen-papers-v2-

      - name: Run digest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
        run: python main.py --save-html

      - name: Upload digest preview
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: digest-preview
          path: data/preview.html
          retention-days: 30

      - name: Save seen papers cache
        uses: actions/cache/save@v4
        if: success()
        with:
          path: data/seen.json
          key: seen-papers-v2-${{ github.run_number }}
